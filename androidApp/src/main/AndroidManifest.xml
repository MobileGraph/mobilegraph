<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:exported="true"
            android:name=".MasterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".RagActivity" />
        <activity android:name=".AgentActivity" />
        <activity android:name=".HitlActivity" />
        <activity android:name=".ParallelAgentsActivity" />
        <activity android:name=".ToolsAgentActivity" />
        <activity android:name=".SubAgentActivity" />
        <activity android:name=".MultiModelActivity" />
        <activity android:name=".ModelRouterActivity" />
        <activity android:name=".AgentRouterActivity" />
        <activity android:name=".McpActivity" />
        <activity android:name=".SkillActivity" />
    </application>

</manifest>